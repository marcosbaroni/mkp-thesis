\Begin{
  $v \leftarrow $ shuffle($1, 2, \ldots, n$)\;
  $x \leftarrow \vec{0}$; \Comment{solução vazia}\\
  \For{$ i \leftarrow 1:n$ }{
    $x[v_i] \leftarrow 1$; \Comment{inserção de item}\\
    \If{ $w(x) > W$ }{
      $x[v_i] \leftarrow 0$; \Comment{verificação de viabilidade}\\
    }
  }
  \textbf{retorna} $s$\;
}