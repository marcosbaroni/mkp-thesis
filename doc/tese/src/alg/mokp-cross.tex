\Kw{$x$: pior indivíduo, $y$: melhor indivíduo, $c$: número de genes herdados}
\Begin{
  $v \leftarrow $ shuffle($1, 2, \ldots, n$)\;
  \For{$i \leftarrow 1:c$ }{
	  $x[v_i] \leftarrow y[v_i];$ \mycomment[10mm]{herança de genes}
  }
  DROP-ADD($x$); \mycomment[10mm]{reparo e complementação de solução}
  Computar aptidão de $x$\;
  \textbf{return} $x$\;
}