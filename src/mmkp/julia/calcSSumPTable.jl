
function calcSSumPTable(nmax, bmax, wmax)
  mat = zeros(nmax, bmax)

  # case for 1
  for b = 1:wmax
       mat[1, b] = 1/wmax
    end
  end

  # case for 2, 3, 4, ..., nmax
  for n = 1:nmax
    for b = 1:nmax*wmax
      s = 
      mat = 
    end
  end
end
