#!/bin/bash
SCDIR="."    # script dir
INPUT=$1
temp=`mktemp`
cplex -c "read $INPUT" "opt" > $temp
tm=`cat $temp | $SCDIR/cplex2time`
gp=`cat $temp | $SCDIR/cplex2gap`
ob=`cat $temp | $SCDIR/cplex2obj`
echo ${ob}";"${gp}";"${tm}
rm $temp

